# Example environment variables file for the carpentry system.
#
# Copy this file to .env and replace the placeholder values with your actual
# Supabase connection details.  Do not commit your real credentials to version
# control.  See README for guidance on sourcing these values.
#
# There are two recommended connection patterns:
#
# 1) Use a pooled connection string (PgBouncer, port 6543).  This is the
#    preferred option when deploying on serverless platforms such as Vercel,
#    because it manages database connections efficiently.  The `pgbouncer=true`
#    and `connection_limit=1` parameters ensure that Prisma opens only a single
#    connection per worker.
#
# DATABASE_URL="postgres://postgres:rolowanieuda6x6QAZ@<POOLER_HOST>:6543/postgres?sslmode=require&pgbouncer=true&connection_limit=1&connect_timeout=15"
#
# 2) Use the direct connection string (port 5432).  This works for local
#    development but may exhaust connection limits on serverless platforms.
#
# DATABASE_URL="postgres://postgres:rolowanieuda6x6QAZ@db.bxfsfzazajspbogfpdtw.supabase.co:5432/postgres?sslmode=require"

# Replace the line below with one of the examples above.
#DATABASE_URL=
